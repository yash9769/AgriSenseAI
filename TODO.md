# TODO: Create Git branch for AgriSenseAI

## Steps:
- [ ] 1. Initialize Git repository (`git init`)
- [ ] 2. Add remote origin (`git remote add origin https://github.com/yash9769/AgriSenseAI.git`)
- [ ] 3. Install GitHub CLI (`gh`) using winget
- [ ] 4. Add all files (`git add .`)
- [ ] 5. Initial commit (`git commit -m "Initial commit from local copy"`)
- [ ] 6. Pull remote main to sync (`git pull origin main --allow-unrelated-histories`)
- [ ] 7. Create new branch (`git checkout -b blackboxai/development`)
- [ ] 8. Push branch to remote (`git push -u origin blackboxai/development`)
- [ ] 9. Verify branch on GitHub (optional: `gh pr create` prep)

Progress will be updated after each step.

